@AGENTS.md

<!--
This file exists only so Claude Code picks up AGENTS.md, which it does not read
by default. Keep the working agreement itself in AGENTS.md so every coding agent
reads the same rules. Add Claude-specific instructions below this comment, not
in AGENTS.md.

Area-specific rules live in .claude/rules/ and load automatically when Claude
opens a file the rule's `paths:` frontmatter covers.
-->
