"""Tests for the rules in this package.

Empty until WP2 moves a rule in. The porting order is test first: write the
Python test from the JavaScript test that covers the rule today, watch it fail,
then move the rule. A rule that arrives without a failing test first cannot be
shown to have arrived intact.
"""
